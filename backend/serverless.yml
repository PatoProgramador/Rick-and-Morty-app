service: rickandMorty-backend
frameworkVersion: '3'

provider:
  name: aws
  runtime: nodejs18.x

functions:
  api:
    handler: index.handler
    events:
      - httpApi: '*'
  api2:
    handler: index2.handler
    events:
      - http:
         path: /
         method: get